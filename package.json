{
  "name": "mutual-agent",
  "version": "1.0.0",
  "description": "MutualBot — Decentralized mutual insurance protocol for AI agents on Moltbook",
  "main": "agent/index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network baseMainnet",
    "agent": "node agent/index.js",
    "agent:once": "SINGLE_RUN=1 node agent/index.js",
    "test": "npx hardhat test"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "axios": "^1.7.0",
    "dotenv": "^16.4.0",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "license": "MIT"
}
