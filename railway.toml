[build]
builder = "NIXPACKS"
buildCommand = "npm install --production && cd lumina-api && npm install --production && cd .."

[deploy]
startCommand = "npm run railway"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 50
